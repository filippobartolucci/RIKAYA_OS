#ifndef _UTILS_H
#define _UTILS_H

#include "pcb.h"
#include "const.h"
#include "listx.h"
#include "types_rikaya.h"


void *memset(void *s, u32 c, int n);
void memcopy(void *dest, void *src, size_t n);

#endif
