#ifndef _UTILS_H
#define _UTILS_H

#include "pcb.h"
#include "const.h"
#include "listx.h"
#include "types_rikaya.h"


void *memset(void *s, u32 c, int n);
void memcpy(void *dest, void *src, u32 n);

#endif
